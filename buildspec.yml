version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --output-template-file outputSamTemplate.yaml --s3-bucket aws-serverless-builds 
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml
